# 更新日志

每次更新发布递增一个补丁版本号（`1.0.1 → 1.0.2`）。最新版本置顶；日期按提交记录整理。此前更新统一使用 `1.0.0`，以下如实补录，不追溯虚构版本号。

## [1.0.6] - 2026-09-15

### 语言支持范围与技术架构文档
- 在 README 中补充已支持和暂不支持的语法表、易混淆写法、运行限制，以及 bool、作用域、整数字面量和类型检查等实际语义差异。
- 标明用户代码的编译入口位于 src/engine.ts，说明页面编译操作与网页构建命令各自的职责。
- 新增可在 GitHub 渲染的 Mermaid 技术架构图，展示源码解析、IR 生成、单步执行、状态快照与 React 可视化的数据流，并补充模块职责和代码结构。

## [1.0.5] - 2026-09-15

### 普通函数定义与调用
- 修复 main 前定义普通函数无法编译的问题，支持 int 返回值、int / bool 按值参数、嵌套和重复调用；`func(1, 2)` 求和示例现在返回 3。
- 单步执行支持 CALL / RET、参数写入和返回值传递；函数栈展示真实教学调用链、当前函数参数与局部变量、入口和返回目标。
- 不同函数可使用同名变量；调用时保留调用方临时值，重复调用重新初始化局部数据，带函数调用的数组下标在复合赋值时只求值一次。
- 增加未定义函数、参数数量不匹配、缺少返回值等诊断，明确提示暂不支持递归、函数原型和重载。
- 补充四种架构的函数调用回归测试、页面单步与返回流程验证，并更新使用指南。

## [1.0.4] - 2026-09-14

### 更直观的函数栈堆叠视图
- 将 main 教学帧和数据槽改为带厚度、层间距的堆叠卡片，区分紫色临时求值栈与绿色局部数据。
- 按高地址到低地址排列求值栈，突出栈底、SP 栈顶和向下增长方向，入栈时新增一层，出栈时移除对应层。
- 栈顶自动保持在求值栈滚动区域内，保留变量读写高亮、地址和字节展示，并支持减少动态效果偏好。

## [1.0.3] - 2026-09-14

### 统一指令流与工作台布局
- 合并重复的指令内存和指令流，集中展示完整程序、指令地址、汇编、源码行号及 PC / IR 标记。
- 指令列表自动跟随执行位置，展开示意图再恢复后仍保持当前指令可见；修改源码时提示上次编译快照。
- 优化桌面单屏布局：上层为指令流、CPU 和函数栈，下层为源码、寄存器状态及变量 / 控制台。
- 调整面板比例、间距和寄存器排版，平板使用双列工作区，手机纵向展示指令流、CPU 和函数栈。

## [1.0.2] - 2026-09-14

### 更直观的 CPU 简单模式
- CPU 示意图默认打开简单模式，以芯片封装、金属引脚和内部电路展示处理器。
- 新增图形化控制单元、寄存器组、ALU、程序计数器与内存接口，实际指令驱动运算结果和数据通路高亮。
- 单步显示一次信号流动，连续运行显示流动的数据和节拍指示；暂停保留快照，结束与异常停止动画。
- 清晰展示取指、译码、执行与写回流程，读写总线按实际数据方向流动。
- 简单模式在手机上纵向排列指令内存、CPU 与函数栈，并支持系统的减少动态效果偏好。

## [1.0.1] - 2026-09-14

### 更新日志与版本管理
- 新增网页更新日志入口，点击顶部版本号或侧栏“更新”即可查看完整历史。
- 页面版本标识、控制台和更新日志窗口统一读取 package.json 的版本号。
- 新增补丁版本递增命令、版本一致性检查，以及 PR 版本递增检查。
- 建立后续每次更新同步版本号与更新日志的项目维护规则。

## [1.0.0] - 2026-09-12 ～ 2026-09-14

### 2026-09-14 · 单屏工作台与函数栈（PR #2）
- 桌面单屏布局：指令内存、CPU、函数栈、源码、指令流、寄存器、控制台和变量同屏展示。
- 优化 1100×700、1366×768、1440×900、1920×1080 视口；长内容在面板内独立滚动。
- 增加简单 / 详细 CPU 模型切换，切换时保留程序执行状态。
- 左侧 Instruction Memory、中间 CPU、右侧 Stack Memory；右侧可切换数据内存。
- main 教学帧显示局部变量、数组、对象成员、地址和字节；临时求值栈显示 SP、栈顶和占用空间。
- 源码自动跟随执行行，CPU 图与编辑器支持展开以及 Escape 恢复。
- 手机和较小视口保留纵向布局。

### 2026-09-12 · CPU 动态示意图
- 新增指令存储器、PC、IR、控制单元、寄存器组、ALU、MAR/MDR、数据内存和总线。
- 根据结构化执行事件动态显示指令地址、源码行、运算前操作数、结果和实际读写地址。
- 高亮地址、数据和控制通路，显示分支目标和是否跳转。
- 支持图示展开、暂停快照以及架构切换 / 重置联动。

### 2026-09-12 · 基础 C++ 类支持（PR #1）
- 修复 class 声明、public 标签和 a.x 成员访问导致编译失败的问题。
- 支持基础 class / struct、独立对象、成员数组、成员表达式、复合赋值和后置自增 / 自减。
- 检查成员访问权限、未知成员、重复声明和对象误用。
- 对象成员按声明顺序分配模拟地址，并接入内存视图和写入高亮。
- 新增“类与对象成员”示例；原始复现程序得到 a.x = 1、a.y = 2、return = 0。

### 2026-09-12 · 首版与在线部署
- 创建浏览器端 C++ 子集解析、IR 编译和 CPU 教学执行引擎。
- 支持 x86、x86-64、ARM32、ARM64 四种教学模型。
- 提供编译、运行、暂停、单步、重置、速度调整、寄存器和小端内存可视化。
- 增加源码高亮、执行轨迹、控制台、结果验证和源代码下载。
- 内置累加求和、斐波那契、冒泡排序、最大公约数、位运算示例。
- 增加除零、数组越界和指令预算诊断，以及引擎 / 浏览器测试。
- 仓库公开并启用 GitHub Pages；main 更新后自动测试、构建和部署。
